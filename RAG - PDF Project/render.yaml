services:
  - type: web
    name: pdf-chrunk-rag
    env: python
    plan: free
    rootDir: RAG - PDF Project
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port=$PORT --server.address=0.0.0.0